import { Camera,Leaf,ShieldCheck,Shirt,Volume2 } from 'lucide-react';import { Container } from '../components/common/Container';import { SectionTitle } from '../components/common/SectionTitle';import { PilgrimNavigation } from '../components/pilgrim/PilgrimNavigation';import { useData } from '../hooks/useSpiritualData';import { pilgrimService } from '../services/pilgrim.service';
const icons={volume:Volume2,shirt:Shirt,camera:Camera,leaf:Leaf,shield:ShieldCheck};
export function EtiquettePage(){const{data}=useData(pilgrimService.rules);return <><div className="bg-forest py-20"><Container><SectionTitle eyebrow="Հարգանքով դեպի սրբավայր" title="Վարքականոնի պարզ կանոններ" light/></Container></div><PilgrimNavigation/><section className="py-20"><Container className="max-w-5xl"><div className="grid gap-4 sm:grid-cols-2">{data?.map((rule,index)=>{const Icon=icons[rule.iconKey as keyof typeof icons]??ShieldCheck;return <article className="border border-gold/25 bg-white/45 p-6" key={rule.id}><div className="flex items-start justify-between"><span className="grid size-12 place-items-center rounded-full bg-forest text-gold"><Icon className="size-5"/></span><span className="font-display text-3xl text-gold/35">0{index+1}</span></div><h2 className="mt-6 font-display text-2xl font-bold text-episcopal">{rule.title}</h2><p className="mt-3 text-sm leading-7 text-ink/60">{rule.description}</p></article>})}</div></Container></section></>}
