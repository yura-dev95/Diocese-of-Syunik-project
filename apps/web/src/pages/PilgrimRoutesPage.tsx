import { Container } from '../components/common/Container';import { LoadingState } from '../components/common/LoadingState';import { SectionTitle } from '../components/common/SectionTitle';import { PilgrimNavigation } from '../components/pilgrim/PilgrimNavigation';import { RouteCard } from '../components/pilgrim/RouteCard';import { useData } from '../hooks/useSpiritualData';import { pilgrimService } from '../services/pilgrim.service';
export function PilgrimRoutesPage(){const{data,isLoading}=useData(pilgrimService.routes);return <><div className="bg-forest py-20"><Container><SectionTitle eyebrow="Պատրաստ երթուղիներ" title="Ուխտագնացության ուղիներ" light/></Container></div><PilgrimNavigation/><section className="py-20"><Container>{isLoading?<LoadingState/>:<div className="grid gap-5 lg:grid-cols-2">{data?.map(r=><RouteCard route={r} key={r.id}/>)}</div>}</Container></section></>}
