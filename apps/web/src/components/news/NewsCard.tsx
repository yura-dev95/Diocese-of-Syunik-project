import { ArrowUpRight,CalendarDays } from 'lucide-react';import { Link } from 'react-router-dom';import { useI18n } from '../../i18n/I18nContext';import type { NewsArticle } from '../../types/news';import { AnimatedCard } from '../common/AnimatedCard';
export function NewsCard({article}:{article:NewsArticle}){const{formatDate,localize}=useI18n();return <AnimatedCard className="group overflow-hidden border border-gold/25 bg-parchment/80"><Link className="focus-ring block" to={`/news-contact/news/${article.slug}`}><div className="relative aspect-[16/10] overflow-hidden bg-episcopal"><img alt={localize(article.title)} className="size-full object-cover opacity-80 transition duration-700 group-hover:scale-105" loading="lazy" src={article.coverUrl}/><div className="absolute inset-0 bg-gradient-to-t from-episcopal via-transparent to-transparent"/><span className="absolute bottom-4 right-4 grid size-11 place-items-center rounded-full bg-parchment text-episcopal"><ArrowUpRight className="size-4"/></span></div><div className="p-6"><div className="flex flex-wrap gap-4 text-[10px] font-bold uppercase tracking-[.14em] text-royal"><span>{localize(article.category)}</span><span className="flex gap-1"><CalendarDays className="size-3"/>{formatDate(article.publishedAt)}</span></div><h2 className="mt-4 font-display text-2xl font-bold text-episcopal">{localize(article.title)}</h2><p className="mt-3 line-clamp-2 text-sm leading-7 text-ink/60">{localize(article.excerpt)}</p></div></Link></AnimatedCard>}
