import { CheckCircle2,LockKeyhole } from 'lucide-react';import { useState,type FormEvent } from 'react';import { newsService } from '../../services/news.service';import { Button } from '../common/Button';import { FormInput,FormTextarea } from '../common/FormInput';
export function ContactForm(){const[form,setForm]=useState({fullName:'',email:'',phone:'',subject:'',message:''});const[error,setError]=useState('');const[reference,setReference]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');if(form.fullName.trim().length<2||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email)||form.subject.trim().length<3||form.message.trim().length<20){setError('Լրացրեք պարտադիր դաշտերը և գրեք առնվազն 20 նիշ պարունակող հաղորդագրություն։');return;}setLoading(true);try{const r=await newsService.contact(form);setReference(r.referenceId)}catch{setError('Հաղորդագրությունը չհաջողվեց ուղարկել։ Խնդրում ենք փորձել կրկին։')}finally{setLoading(false)}}if(reference)return <div className="border border-forest/30 bg-white/55 p-8 text-center"><CheckCircle2 className="mx-auto size-10 text-forest"/><h2 className="mt-5 font-display text-3xl font-bold text-episcopal">Հաղորդագրությունն ընդունված է</h2><p className="mt-3 text-sm text-ink/55">Հղման համարը՝ {reference.slice(0,8)}</p></div>;return <form className="border border-gold/25 bg-white/45 p-6 shadow-sacred sm:p-9" onSubmit={submit}><div className="mb-6 flex gap-3 border border-gold/25 bg-parchment/70 p-4 text-xs leading-6 text-ink/55"><LockKeyhole className="size-4 shrink-0 text-forest"/>Ձեր կոնտակտային տվյալները չեն հրապարակվում և օգտագործվում են միայն պատասխանելու համար։</div><div className="grid gap-5 sm:grid-cols-2"><FormInput label="Անուն, ազգանուն *" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/><FormInput label="Էլ. հասցե *" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><FormInput label="Հեռախոս" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><FormInput label="Թեմա *" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/></div><div className="mt-5 grid gap-5"><FormTextarea label="Հաղորդագրություն *" rows={7} value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/>{error&&<p className="text-xs text-royal">{error}</p>}<Button disabled={loading} type="submit">{loading?'Ուղարկվում է...':'Ուղարկել հաղորդագրությունը'}</Button></div></form>}
