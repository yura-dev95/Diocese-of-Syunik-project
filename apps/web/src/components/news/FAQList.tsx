import { ChevronDown } from 'lucide-react';import { useState } from 'react';import type { FAQ } from '../../types/news';
export function FAQList({items}:{items:FAQ[]}){const[open,setOpen]=useState<string>();return <div className="grid gap-3">{items.map(item=><article className="border border-gold/25 bg-white/45" key={item.id}><button aria-expanded={open===item.id} className="focus-ring flex min-h-16 w-full items-center justify-between gap-5 px-5 text-left font-display text-lg font-bold text-episcopal" onClick={()=>setOpen(open===item.id?undefined:item.id)} type="button"><span>{item.question}</span><ChevronDown className={`size-4 shrink-0 transition ${open===item.id?'rotate-180':''}`}/></button>{open===item.id&&<p className="border-t border-gold/20 px-5 py-5 text-sm leading-7 text-ink/60">{item.answer}</p>}</article>)}</div>}
