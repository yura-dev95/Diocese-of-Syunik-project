import { Bell,HelpCircle,Images,Mail,Newspaper } from 'lucide-react';import { NavLink } from 'react-router-dom';import { useI18n } from '../../i18n/I18nContext';import { Container } from '../common/Container';
const links=[['/news-contact','news.news',Newspaper,true],['/news-contact/announcements','news.announcements',Bell],['/news-contact/gallery','news.gallery',Images],['/news-contact/faq','news.faq',HelpCircle],['/news-contact/contact','news.contact',Mail]] as const;
export function NewsNavigation(){const{t}=useI18n();return <div className="border-b border-gold/20 bg-parchment"><Container className="flex gap-2 overflow-x-auto py-4">{links.map(([to,labelKey,Icon,end])=><NavLink className={({isActive})=>`focus-ring flex min-h-11 shrink-0 items-center gap-2 rounded-full px-4 text-xs font-bold ${isActive?'bg-episcopal text-parchment':'border border-gold/30 text-episcopal'}`} end={end??false} key={to} to={to}><Icon className="size-4"/>{t(labelKey)}</NavLink>)}</Container></div>}
