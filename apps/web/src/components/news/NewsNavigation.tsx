import { Bell,HelpCircle,Mail,Newspaper } from 'lucide-react';import { NavLink } from 'react-router-dom';import { useI18n } from '../../i18n/I18nContext';import { Container } from '../common/Container';
const links=[['/news-contact','news.news',Newspaper,true],['/news-contact/announcements','news.announcements',Bell],['/news-contact/faq','news.faq',HelpCircle],['/news-contact/contact','news.contact',Mail]] as const;
export function NewsNavigation(){const{t}=useI18n();return <div className="border-y border-gold/20 bg-parchment/90"><Container className="flex gap-0 overflow-x-auto py-4">{links.map(([to,labelKey,Icon,end])=><NavLink className={({isActive})=>`focus-ring flex min-h-12 shrink-0 items-center gap-2 border border-gold/25 px-5 text-xs font-bold transition first:rounded-l-md last:rounded-r-md ${isActive?'bg-gold text-white':'bg-parchment/70 text-episcopal hover:bg-white/75'}`} end={end??false} key={to} to={to}><Icon className="size-4"/>{t(labelKey)}</NavLink>)}</Container></div>}
