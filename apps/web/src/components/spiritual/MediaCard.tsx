import { Pause, Play } from 'lucide-react';
import { useState } from 'react';
import type { MediaItem } from '../../types/spiritual';
import { AnimatedCard } from '../common/AnimatedCard';
const labels={SERMON:'Քարոզ',PODCAST:'Սյունյաց շունչ',VLOG:'Տեսապատում',SHARAKAN:'Շարական',CHOIR_RECORDING:'Երգչախումբ'};
export function MediaCard({item}:{item:MediaItem}){const[playing,setPlaying]=useState(false);return <AnimatedCard className="overflow-hidden border border-gold/25 bg-white/45"><div className="relative aspect-video overflow-hidden bg-episcopal"><img alt={item.title} className="size-full object-cover opacity-65" loading="lazy" src={item.coverUrl}/><button aria-label={playing?'Դադարեցնել':'Լսել'} className="focus-ring absolute left-1/2 top-1/2 grid size-14 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-gold text-episcopal" onClick={()=>setPlaying(!playing)} type="button">{playing?<Pause className="size-5 fill-current"/>:<Play className="ml-1 size-5 fill-current"/>}</button></div><div className="p-6"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-royal">{labels[item.mediaType]} · {item.duration}</p><h3 className="mt-3 font-display text-2xl font-bold text-episcopal">{item.title}</h3><p className="mt-3 text-sm leading-7 text-ink/60">{item.description}</p></div></AnimatedCard>}
